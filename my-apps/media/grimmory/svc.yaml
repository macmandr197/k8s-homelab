apiVersion: v1
kind: Service
metadata:
  name: grimmory
  namespace: grimmory
spec:
  type: ClusterIP
  selector:
    app: grimmory
  ports:
    - name: http
      port: 6060
      targetPort: 6060
      protocol: TCP
