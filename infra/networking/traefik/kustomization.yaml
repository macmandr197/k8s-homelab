apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ns.yaml
  - external-secret.yaml
  - external-services/adguard-http-route.yaml


helmCharts:
  - name: traefik
    repo: https://traefik.github.io/charts
    version: 39.0.7
    releaseName: traefik
    includeCRDs: true
    namespace: traefik
    valuesFile: values.yaml

generatorOptions:
  disableNameSuffixHash: true
