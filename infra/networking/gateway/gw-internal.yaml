apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-internal
  namespace: gateway
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-cluster-issuer
  labels:
    bgp.cilium.io/advertise-service: default #to match the BGP Advertise selector
spec:
  addresses:
  - type: IPAddress
    value: 172.16.18.2
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.labber.mcmn.io"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.labber.mcmn.io"
      tls:
        mode: Terminate
        certificateRefs:
          - name: cert-labber-mcmn
            kind: Secret
            group: "" # required
        # No QUIC/HTTP3 for internal gateway - only HTTP/2 and HTTP/1.1
        options:
          gateway.networking.k8s.io/alpn-protocols: "h2,http/1.1"
      allowedRoutes:
        namespaces:
          from: All
    # TCP listener for PostgreSQL
    - name: postgres
      protocol: TCP
      port: 5432
      allowedRoutes:
        namespaces:
          from: Same