apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: wger-daily-backup
  namespace: wger
spec:
  # Sec Min Hour Day Month DayOfWeek
  schedule: "0 0 2,14 * * *"
  backupOwnerReference: self
  cluster:
    name: wger-db
  immediate: true
